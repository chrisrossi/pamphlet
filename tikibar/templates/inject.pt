<script>
  var tikibar = function() {

    function set_height(height) {
      $('#tikibar').height(height + 'px');
    }

    // Inject tikibar iframe
    var tikibar_url = '${tikibar_url}';
    $(document).ready(function() {
      $('<iframe>')
        .attr('id', 'tikibar')
        .attr('src', tikibar_url)
        .attr('seamless', '')
        .attr('scrolling', 'no')
        .attr('frameborder', '0')
        .css('border', 'none')
        .css('display', 'block')
        .width('100%')
        .height('50px')
        .prependTo('body');
    });

    return {
      set_height: set_height
    }
  }();
</script>
